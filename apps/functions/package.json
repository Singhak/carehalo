{
  "name": "functions",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p ../../libs/tsconfig.json && tsc",
    "watch": "tsc -w",
    "prepare": "npm run build",
    "start:emulator": "npm run build && firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "@types/pdfkit": "^0.17.3",
    "express": "^4.18.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^4.4.0",
    "pdfkit": "^0.17.2",
    "puppeteer": "^24.32.1"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
